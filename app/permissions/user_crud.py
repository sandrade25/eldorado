from app.utils.depends import Permissions

can_create_user = Permissions(["can create user"])
can_delete_user = Permissions(["can delete user"])
can_edit_user = Permissions(["can edit user"])
can_view_other_user_data = Permissions(["can view other user data"])
